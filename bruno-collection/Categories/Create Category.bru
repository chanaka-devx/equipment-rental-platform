meta {
  name: Create Category
  type: http
  seq: 1
}

post {
  url: {{base_url}}/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "Heavy Machinery"
  }
}
